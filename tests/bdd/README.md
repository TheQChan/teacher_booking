## BDD-тесты Teacher Booking

Поместите сценарии на `Gherkin` и шаги для `behave`, `pytest-bdd` или другого BDD-фреймворка сюда. Держите отдельное окружение/зависимости, если требуется (например, `requirements-bdd.txt`), и добавьте инструкции запуска в этот файл.
